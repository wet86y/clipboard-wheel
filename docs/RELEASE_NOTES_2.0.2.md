# 超级中键 2.0.2

本版本修复了部分本地文件夹可能让 Windows Shell 调用不返回，进而堵住整个突破动作队列的问题。

- 文件夹快捷方式改由独立 STA 辅助进程执行，并恢复默认 Shell 动词与 `NOASYNC/DDEWAIT` 语义。
- 辅助进程连续 3 秒未返回时开始记录挂起诊断；主程序最多等待 5 秒便回收辅助进程，单个异常目录不能再永久阻塞后续突破动作。
- 权限重启退出时提前释放单实例互斥体，避免新进程因旧进程后台清理而接管超时。
- 正式 Release 仍为单 EXE，不包含诊断日志、PDB 或故障注入。
